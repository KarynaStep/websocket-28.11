// const {Message }= require('../models')

// module.exports.addMessage = async (newMessage) => {
//     try {
//       const savedMessage = await Message.create(newMessage);
//       io.emit('newMessage', savedMessage);
//     } catch (error) {
//       socket.emit("errorMessade", error);
//     }
//   };
// };